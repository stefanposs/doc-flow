[project]
name = "docflow"
version = "0.1.0"
description = "End-to-End Document Text Extraction Pipeline — PDF, Office, Images → Markdown"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
authors = [{ name = "Stefan Poss" }]
keywords = ["document", "extraction", "ocr", "pdf", "markdown", "pipeline"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: FastAPI",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing",
]

dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
    "pydantic>=2.10,<3",
    "pydantic-settings>=2.7,<3",
    "httpx>=0.28,<1",
    "python-multipart>=0.0.18,<1",
    "structlog>=24.4,<26",
    "python-magic>=0.4,<1",
]

[project.optional-dependencies]
tika = []  # Tika uses httpx (already in deps)
pymupdf = ["pymupdf>=1.25,<2"]
tesseract = ["pytesseract>=0.3,<1", "Pillow>=11,<12"]
s3 = ["boto3>=1.35,<2"]
redis = ["redis>=5,<6"]
gcs = ["google-cloud-storage>=2,<3"]
azure = ["azure-storage-blob>=12,<13"]
llm-openai = ["openai>=1.60,<2"]
llm-ollama = ["ollama>=0.4,<1"]
all = ["docflow[pymupdf,tesseract,s3,redis,gcs,azure,llm-openai,llm-ollama]"]
dev = [
    "pytest>=8.3,<9",
    "pytest-asyncio>=0.24,<1",
    "pytest-cov>=6,<7",
    "pytest-xdist>=3.5,<4",
    "pytest-timeout>=2.3,<3",
    "respx>=0.22,<1",
    "ruff>=0.9,<1",
    "mypy>=1.14,<2",
    "pre-commit>=4,<5",
]
docs = [
    "mkdocs-material>=9.5,<10",
    "mkdocs-minify-plugin>=0.8,<1",
    "mkdocstrings[python]>=0.27,<2",
]

[project.urls]
Homepage = "https://github.com/stefanposs/doc-flow"
Documentation = "https://stefanposs.github.io/doc-flow"
Repository = "https://github.com/stefanposs/doc-flow"
Issues = "https://github.com/stefanposs/doc-flow/issues"

[project.scripts]
docflow = "docflow.adapters.inbound.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/docflow"]

# ─── Ruff ────────────────────────────────────────────────────
[tool.ruff]
target-version = "py312"
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "W",     # pycodestyle, pyflakes
    "I",                # isort
    "N",                # pep8-naming
    "UP",               # pyupgrade
    "S",                # bandit (security)
    "B",                # bugbear
    "A",                # builtins
    "C4",               # comprehensions
    "DTZ",              # datetime timezone
    "T20",              # print statements
    "PT",               # pytest style
    "SIM",              # simplify
    "TID",              # tidy imports
    "TCH",              # type checking
    "ARG",              # unused arguments
    "PL",               # pylint
    "TRY",              # tryceratops
    "PERF",             # performance
    "RUF",              # ruff-specific
]
ignore = ["S101", "TRY003", "PLR0913"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
    "PLR2004",   # magic values in assertions
    "PLC0415",   # local imports (lazy test setup)
    "ARG002",    # unused args (interface contracts in fakes)
    "S110",      # try-except-pass
]
"src/**/dependencies.py" = ["PLC0415", "PERF401"]
"src/**/worker.py" = ["TRY300", "PERF401", "SIM105", "S110"]
"src/**/job_service.py" = ["PLC0415"]
"src/**/job_routes.py" = ["PLC0415"]
"src/**/cli.py" = ["PLC0415"]
"src/**/extractors/*.py" = ["PLC0415"]
"src/**/ocr/*.py" = ["PLC0415"]
"src/**/blob/*.py" = ["PLC0415", "TRY300"]
"src/**/queue/*.py" = ["PLC0415", "SIM105", "S110"]
"src/**/cache/*.py" = ["PLC0415"]
"src/**/storage/__init__.py" = ["ARG002"]
"src/**/formatters/plaintext.py" = ["ARG002"]
"src/**/formatters/markdown.py" = ["PLR2004"]
"src/**/processors/cleanup.py" = ["ARG002"]
"src/**/pipeline.py" = ["PLR2004"]
"src/**/*schemas*.py" = ["TC003"]  # Pydantic needs datetime at runtime
"src/**/extractors/tika.py" = ["PLR2004"]

[tool.ruff.lint.isort]
known-first-party = ["docflow"]

# ─── mypy ────────────────────────────────────────────────────
[tool.mypy]
python_version = "3.12"
strict = true
disallow_untyped_defs = true
warn_return_any = true
warn_unused_configs = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["pytesseract.*", "magic.*", "pymupdf.*", "fitz.*", "boto3.*", "botocore.*", "google.cloud.*", "google.auth.*", "redis.*", "azure.*"]
ignore_missing_imports = true

# ─── pytest ──────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "unit: Fast tests without external dependencies",
    "integration: Tests requiring Docker services (Tika, etc.)",
    "e2e: End-to-end pipeline tests",
]
addopts = "--strict-markers --tb=short -q"

[tool.coverage.run]
source = ["src/docflow"]
omit = ["*/tests/*", "*/__main__.py"]

[tool.coverage.report]
fail_under = 40
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@abstractmethod",
]
